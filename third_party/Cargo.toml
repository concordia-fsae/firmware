[workspace]

[package]
name = "rust-third-party"
version = "0.0.0"
publish = false
edition = "2021"

# Dummy target to keep Cargo happy
[[bin]]
name = "fake"
path = "/dev/null"

[dependencies]
clap = { version = "4", features = ["derive"] }
libc = "0.2.175"
socketcan = "3.5.0"
