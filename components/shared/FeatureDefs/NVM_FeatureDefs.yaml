config:
  prefix: nvm
  description: General definitions for non voltaile memory
defs:
  lib_enabled:
  block_size:
  task:
    requires:
      nvm_lib_enabled:
  flash_backed:
    requires:
      - nvm_lib_enabled
      - nvm_block_size
