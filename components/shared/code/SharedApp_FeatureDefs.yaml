config:
  prefix: feature
  description: General definitions for applications and bootloaders
defs:
  vehicleState_mode:
    discreteValue: mode
  vehicleSpeed_leader:
discreteValues:
  mode:
    - leader
    - follower
