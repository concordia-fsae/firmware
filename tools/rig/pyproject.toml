[project]
name = "rig"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["clang==18.1.8", "pytest>=8"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["rig"]
package-dir = {rig = "python"}
